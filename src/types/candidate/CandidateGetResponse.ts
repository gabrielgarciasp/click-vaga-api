export type CandidateGetResponse = {
    email: string,
    name: string,
    gender: string,
    phone: string,
    birthDate?: Date,
    picture?: string,
    cep?: string,
    address?: string,
    number?: string,
    district?: string,
    city?: string,
    state?: string,
}
